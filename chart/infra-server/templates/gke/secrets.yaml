---

apiVersion: v1
kind: Secret
type: Opaque

metadata:
  name: google-credentials
  namespace: default

data:
  google-credentials.json: |-
    {{- include "require-file" (list "gke/gke-credentials.json" .) | b64enc | nindent 4 }}

---
