/**
 * This Tailwind config is a copy from the stackrox/rox repo.
 * TODO: consolidate by extracting tailwind stuff into a separate NPM package.
 */

module.exports = {
  theme: {
    extend: {},
  },
  variants: {},
  plugins: [],
};
